# A股证券化率 — Buffett Indicator Tracker

> 巴菲特指标（证券化率）= A股全市场总市值 ÷ 中国GDP  
> 自动追踪，每日更新，Excel输出

---

## 目录

- [指标概述](#指标概述)
- [快速开始](#快速开始)
- [历史规律](#历史规律)
- [项目架构](#项目架构)
- [数据源 & 反封策略](#数据源--反封策略)
- [新增指标](#新增指标)
- [CLI 命令](#cli-命令)
- [文件说明](#文件说明)

---

## 指标概述

**巴菲特指标**（又称证券化率）是巴菲特在《Fortune》杂志2001年文章中提出的估值指标：

```
证券化率 = 全市场股票总市值 / 国民生产总值(GDP)
```

| 区间 | 含义 |
|------|------|
| < 50% | 显著低估 |
| 50% ~ 75% | 适度低估 |
| 75% ~ 90% | 合理区间 |
| 90% ~ 120% | 偏高 |
| > 120% | 明显高估 |
| > 200% | 泡沫状态 |

> ⚠️ 以上区间是针对美国市场的经验值。A股由于IPO制度、上市公司结构不同，阈值会有所偏移。

### GDP外推规则

每年的GDP通常在次年1月公布全年数据。对于还没公布全年数据的年份，按以下规则**线性外推**：

1. 有全年GDP → 直接用
2. 有前3季度GDP → (Q1+Q2+Q3) / 3 × 4
3. 有上半年GDP → (Q1+Q2) × 2
4. 有Q1 → Q1 × 4
5. 都没有 → 用上年全年GDP

---

## 快速开始

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 运行（计算今天并追加到Excel）
python main.py

# 3. 指定日期
python main.py --date 2024-12-31

# 4. 查看所有指标
python main.py --list
```

输出文件：`output/A股证券化率.xlsx`

### 每日使用

```bash
# Windows 定时任务 / 手动运行
python main.py

# 日志输出示例：
# 2026-07-25 | 总市值=105.93万亿 | GDP=141.00万亿(无2026数据，使用2025年全年GDP) | 证券化率=75.13%
```

已有数据的日期会自动跳过，不会重复请求API。

---

## 历史规律

Excel 中预置了2005年至今每半月一条的数据，以及10个经典牛熊参考日：

| 日期 | 类型 | 上证点位 | 证券化率 |
|------|------|---------|---------|
| 2007-10-16 | 🐂 牛市顶点 | 6124 | **475.86%** |
| 2008-10-28 | 🐻 熊市底部 | 1664 | **119.05%** |
| 2009-08-04 | 🐂 牛市顶点 | 3478 | **223.80%** |
| 2013-06-25 | 🐻 熊市底部 | 1849 | **81.06%** |
| 2015-06-12 | 🐂 牛市顶点 | 5178 | **204.82%** |
| 2016-01-27 | 🐻 熊市底部 | 2638 | **100.97%** |
| 2019-01-04 | 🐻 熊市底部 | 2440 | **65.20%** |
| 2021-02-18 | 🐂 牛市顶点 | 3731 | **90.64%** |
| 2024-02-05 | 🐻 熊市底部 | 2635 | **51.99%** |
| 2024-10-08 | 🐂 牛市顶点 | 3674 | **70.35%** |

**当前：75.13%** — 处于历史中枢偏下位置。

---

## 项目架构

```
a-stock-indicator/
├── main.py                    # 入口：自动发现指标、CLI
├── core/                      # 基础设施（共享）
│   ├── cache.py               # JSON缓存 + TTL过期
│   ├── data_fetcher.py        # HTTP请求（重试+退避+UA轮换+限流+熔断）
│   ├── base_indicator.py      # 指标基类（继承即自动注册）
│   └── excel_writer.py        # Excel追加/更新
├── indicators/                # 指标实现（自动发现）
│   └── buffett_indicator.py   # 证券化率
├── data_sources/              # 数据源（各指标共享）
│   ├── market_cap.py          # A股总市值
│   └── gdp.py                 # 中国GDP
├── output/                    # 输出目录
│   └── A股证券化率.xlsx       # 计算结果
└── requirements.txt
```

### 核心设计原则

- **自动发现**：放在 `indicators/` 目录下、继承 `BaseIndicator` 的类自动被发现
- **多级降级**：每个数据源有2~3层备用源，主源被封自动切换
- **请求限流**：按域名独立限流，腾讯0.3s、东财2.0s间隔
- **熔断保护**：连续2次连接失败后该域名熔断120秒
- **本地缓存**：GDP数据24h缓存，K线数据进程内缓存
- **幂等写入**：同一日期的数据重复运行只更新不重复

---

## 数据源 & 反封策略

### A股总市值

| 优先级 | 源 | 协议 | 封IP风险 |
|--------|-----|------|---------|
| 1 | 腾讯财经 `qt.gtimg.cn` | HTTP | **不封IP** ✅ |
| 2 | 东财 push2 API | HTTP | 有风控 |

- **今日实时**：直接查询上证综指+深证综指+北证50的总市值字段
- **历史推算**：通过腾讯K线接口获取历史指数点位，按比例缩放各交易所市值

### GDP

| 优先级 | 源 | 说明 |
|--------|-----|------|
| 1 | 硬编码基线 | 2005~2025年官方GDP数据 |
| 2 | NBS easyquery API | 国家统计局公开接口（自动刷新） |

- GDP每年更新4次，变化不频繁，硬编码基线为主
- 版本号管理：基线数据变更后缓存自动失效

### 反封措施

| 措施 | 实现 |
|------|------|
| UA轮换 | 5个真实浏览器UA，每次请求随机切换 |
| 域名限流 | 腾讯0.3s，东财2.0s |
| 指数退避 | 失败后 1s → 2s → 4s 递增等待 |
| 熔断器 | 同一域名连续2次失败后暂停120s |
| 优先不封IP源 | 腾讯优先于东财 |

---

## 新增指标

在 `indicators/` 目录下新建 `.py` 文件，继承 `BaseIndicator` 即可自动发现。

```python
from core.base_indicator import BaseIndicator

class MyIndicator(BaseIndicator):
    @property
    def name(self) -> str:
        return "我的指标"        # → 输出 output/我的指标.xlsx

    @property
    def columns(self) -> list[str]:
        return ["日期", "数值A", "数值B", "备注"]

    def compute(self, target_date):
        # 计算逻辑...
        return {
            "日期": target_date,
            "数值A": 100.0,
            "数值B": 200.0,
            "备注": "",
        }
```

无需修改任何配置，`python main.py` 会自动发现并运行。

---

## CLI 命令

```bash
# 运行所有指标（今天）
python main.py

# 运行指定日期
python main.py --date 2024-12-31

# 只跑某个指标
python main.py --indicator buffett_indicator

# 查看所有可用指标
python main.py --list

# 静默模式
python main.py --quiet
```

---

## 文件说明

| 文件 | 大小 | 说明 |
|------|------|------|
| `output/A股证券化率.xlsx` | ~24KB | 最终输出，含536条历史+每日数据 |
| `~/.a_stock_indicator/cache/` | 自动创建 | GDP缓存、K线缓存 |

---

## 依赖

```
requests
pandas
openpyxl
```

零第三方金融数据依赖，所有数据直连公开HTTP API。
